import { sendToDB } from "./extCommMod.js";

/**
 * Temporary stub method to get and send dietary preferences from the database.
 * 
 * @param {object} userParams User's new preferences
 * @returns User's current preferences
 */
export const dietaryManagement = userParams => sendToDB(null);